# Northlight RealCapture — UAT and audit report

**Audit window:** 22–23 August 2026  
**Environment:** production deployment at `northlight-realcapture.pages.dev`  
**Branch:** `northlight-production`  
**UAT run:** `uat-20260822151051`  
**Verdict:** demo-ready for Pankaj with Xero explicitly deferred; not yet “flawless production” until the remaining polish/ops items below are closed.

This report intentionally does **not** include passwords, provider tokens, secure file URLs, OAuth codes, or secret values.

## Executive summary

Northlight RealCapture is now a credible production-grade demo for the core RealCapture story:

- one property record;
- role-aware workflow;
- photographer-owned availability/calendar;
- Dropbox-backed secure media;
- revision loop;
- approved immutable delivery release;
- Admin/Owner/Agent/Photographer/Editor access checks;
- connected Google Workspace and Dropbox health;
- calm, premium, mostly clear UI across desktop/tablet/mobile.

During UAT, five material blockers/UX breakers were found and fixed:

1. Admin bootstrap failed against production integration schema drift.
2. Review publication failed when older edited Dropbox files remained in the source folder.
3. Approved release temporary file links failed for all roles.
4. Admin deactivate/reactivate returned 500 after actually changing account state.
5. Photographer “save availability” returned a generic 400 when saving an unchanged schedule.

All five fixes were committed, pushed and production-verified. A final UI polish pass then closed four additional confidence issues: post-action refresh, Dropbox index wording, sidebar landmark labelling and approved-release presentation.

## Release/demo decision

### Demo readiness

**Ready for demo** if the demo script is framed as:

- Dropbox and Google Workspace are live.
- Xero is intentionally deferred until Pankaj grants access.
- UAT data is synthetic.
- Product is still in polish/hardening pass, not declared final customer production.

### Remaining caveats

- Xero actual connection/invoice/webhook testing is deferred.
- Native Chrome file picker upload could not be fully automated by the browser extension; the backend upload, Dropbox transfer, indexing, review, delivery and approved-link path was tested live.
- Temporary UAT credentials/tokens should be rotated/revoked after the demo.

## Fixes shipped during this UAT pass

| Commit | Fix | Verification |
|---|---|---|
| `1133f53` | Fixed Admin bootstrap integration schema drift by avoiding missing `integration_state.updated_at`. | Production Admin bootstrap returned 200 after deploy. |
| `73cd2f2` | Allowed review publication when extra unselected edited source files exist in Dropbox. | Revision review submit succeeded; task moved to review with immutable release. |
| `de2d830` | Switched approved release temporary-link route away from mutable provider id assumptions. | First-stage fix deployed; uncovered need for metadata verification. |
| `6245a45` | Verified approved release links against Dropbox metadata path before returning secure links. | Admin, Owner, Agent, Photographer and Editor all received valid approved-release links. |
| `7cd99f4` | Kept Admin user updates successful even when non-task audit logging fails. | Production deactivate/reactivate returned 200 after deploy. |
| `f987f97` | Made unchanged Photographer availability saves idempotent. | Production unchanged save returned 200 with `reused: true`; real change/revert already returned 200. |
| `cbb3cc3` | Added authoritative task refetch after mutations, calm Dropbox index wording, an accessible workspace landmark, and protected approved-release UI with safe release metadata. | Production attested the exact commit; signed-in Admin Chrome verified release `32E6605D`, approval time, file count, immutable-source microcopy and revised activity wording. |
| `009f119` | Hardened the exact-byte production asset gate with bounded cache-busted retries. | A real deploy briefly exposed new health/build attestation while one edge still served prior CSS; the hardened gate kept exact-byte comparison and the complete GitHub CI rerun passed. |

Full certification after the latest fix:

- `npm run check` passed.
- 193 tests passed.
- Production dispatcher dry-run passed.
- Preview dispatcher dry-run passed.
- GitHub Northlight CI passed, including production commit attestation, public smoke checks, real-Chrome rendering and exact static-asset byte parity.

## Live UAT evidence

### Core task workflow

Primary task:

- Task no: `NL-243673-46FB`
- Property: `UAT Calendar Postfix uat-20260822151051`
- Final status: `delivered`
- Approved release id: `32e6605d-6a43-482a-83db-b78a9da54620`
- Approved file: `uat-uat-20260822151051-revision-living-warm.png`

Workflow exercised:

1. Admin/management view.
2. Dropbox reconnect.
3. Google Workspace reconnect.
4. Backend direct upload to task edited media.
5. Submit review.
6. Request revision.
7. Upload revised edited file.
8. Submit final review.
9. Approve delivery.
10. Generate approved release temporary links.
11. Verify post-delivery workflow actions are blocked.

### Role matrix

All five synthetic roles could sign in and access the delivered task appropriately:

| Role | Login | Sees delivered task | Approved release link | Wrong lifecycle/role actions |
|---|---:|---:|---:|---|
| Admin | Pass | Pass | Pass | Blocked with 409 lifecycle messages |
| Owner | Pass | Pass | Pass | Blocked with 409 lifecycle messages |
| Agent | Pass | Pass | Pass | Role/lifecycle blocks correct |
| Photographer | Pass | Pass | Pass | Role/lifecycle blocks correct |
| Editor | Pass | Pass | Pass | Role/lifecycle blocks correct |

Negative workflow checks after delivery:

- `approve_delivery` returns “not currently awaiting final review.”
- `request_revision` returns “not currently awaiting review.”
- `submit_review` returns “editing must be in progress” or role-specific 403.
- `source_ready` returns “source media cannot be handed to editing” or role-specific 403.

### Integrations

| Provider | Status | Evidence |
|---|---|---|
| Dropbox | Connected | Account label visible; live verify endpoint passed; scopes include account info, file content read/write and metadata read. |
| Google Workspace | Connected | Account label visible; live verify endpoint passed; Gmail compose/userinfo/openid grants present. |
| Xero | Not connected | Intentionally deferred until Pankaj provides access. |
| Photographer personal Google Calendar | Connected for UAT photographer | Admin availability/profile view shows connected personal calendar and readiness for calendar checks. |

### Admin user lifecycle

Disposable UAT account tested:

- invalid email rejected with 400;
- short starter password rejected with 400;
- new Agent creation returned 201;
- duplicate email returned 409;
- login while active returned 200;
- deactivate now returns 200 and blocks login;
- reactivate now returns 200 and restores login.

The important fixed bug: before `7cd99f4`, deactivate/reactivate returned 500 even though state changed. That is now production-fixed.

### Photographer availability and calendar control

Photographer-owned availability was tested directly:

- Admin cannot use the self-service availability endpoint: 403.
- Photographer can read profile and onboarding state: 200.
- Invalid overnight working hours are rejected with a field-specific 400.
- A real availability change succeeds and increments the version.
- Reverting the change succeeds and restores the original hours.
- Saving an unchanged schedule now returns 200 with `reused: true` instead of a confusing generic 400.

This supports the product philosophy: photographers control their own slots/calendar availability, while Admin/Owner views coverage and readiness.

## UI/UX and visual audit

### Positive findings

- The login screen is premium, quiet and clear.
- Admin dashboard communicates “exceptions and handoffs come to you” well.
- Tasks page supports the product philosophy: one property, one record.
- Integrations cards clearly show:
  - current provider;
  - connection status;
  - supported Northlight → provider direction;
  - supported provider → Northlight direction;
  - verify/reconnect/disconnect actions.
- Availability page explains the correct control split:
  - photographers own working hours and personal Calendar;
  - management owns service/area coverage.
- Mobile/tablet layouts do not horizontally overflow.
- Mobile More menu works and exposes secondary Admin destinations.
- Mobile task drawer opens and shows booking, people, secure media, conversation, issues, activity and lifecycle.
- Delivered media appears in Final, with approved release link support.
- Delivered media now includes an explicit protected approved-release card with approval time, short release reference, file count and immutable-source explanation.
- Dropbox root-index retirement is presented as an index refresh; genuine file deletion remains distinct.
- Revision, issue and standard workflow mutations now refetch authoritative task state instead of routinely reloading the page.
- No application console warnings/errors were observed during the responsive pass or final deployed verification. Chrome did report browser-extension message-channel noise, which was not emitted by Northlight code.
- Accessibility live DOM pass found:
  - no duplicate IDs;
  - labelled form fields;
  - all visible controls named;
  - skip link present and target exists;
  - live regions for status/search feedback.

### Resolved in the final polish pass

| Issue | Production result |
|---|---|
| Post-action UI state could require reload. | Central task refresh disables stale actions, reloads bootstrap truth and reopens the fresh task. Revision and issue modals use the same path. |
| Dropbox reconnect activity looked like data loss. | Root index retirement now reads “Dropbox index refreshed”; actual provider file absence remains explicitly different. |
| Sidebar landmark lacked a label. | Deployed `<aside>` is exposed as “Northlight workspace”. |
| Approved release semantics were understated. | Final media shows Protected, approval time, short release reference, file count and source-folder immutability. |

### UX issues to fix before calling it flawless

| Priority | Issue | Why it matters | Recommendation |
|---|---|---|---|
| P1 | Legacy `admin@northlight.local` still hits forced password setup. | Demo confusion if anyone uses the visible seed account. | Retire it, migrate it, or hide it from demo docs. Use Admin-created credentials as the normal product path. |
| P2 | Integration health could be even more confidence-building. | Owners/Admins want truth at a glance. | Add “last sync / next retry / last provider event / reconnect needed” row per provider. |
| P3 | Visual delight can be lifted. | The base is calm; premium feel can become memorable. | Add small SVG status marks, subtle transitions, richer empty states, improved skeletons and warm property/media thumbnails. |

## Architecture/integration assessment

The current integration architecture is directionally correct.

### Dropbox

Dropbox webhooks are only a wake-up signal; they do not include the actual file changes. The durable design should keep a cursor and call `files/list_folder/continue` to reconcile truth. Northlight is already aligned with this pattern through cursor/state/retry tests and Dropbox sync logic.

Sources:

- https://www.dropbox.com/developers/reference/webhooks
- https://developers.dropbox.com/detecting-changes-guide

### Google Calendar

Google Calendar also needs both notification channels and incremental sync tokens. Push notifications improve timeliness, but missed/out-of-order events must be recovered by sync-token based reconciliation. Northlight’s watch/channel/cursor tests match this pattern.

Sources:

- https://developers.google.com/workspace/calendar/api/guides/push
- https://developers.google.com/workspace/calendar/api/guides/sync

### Xero

Xero should be treated as deferred but architecturally similar: signed webhook intake, idempotent event handling, follow-up API fetch, retry-safe invoice creation/update, and reconciliation after ambiguous provider failures.

Source:

- https://developer.xero.com/documentation/guides/webhooks/overview/

## Competitor/product positioning

Competitors such as Tonomo, Aryeo and HDPhotoHub emphasize booking, scheduling, project management, delivery, client portals, upsells and calendar integration. Northlight’s opportunity is to be less noisy and more operationally trustworthy:

- exception-first workflow instead of dashboard clutter;
- one property command center;
- role-specific “what do I do next?” screens;
- visible integration truth and recovery;
- photographer-controlled calendar;
- immutable approved delivery releases;
- owner/admin confidence without exposing unnecessary controls to field roles.

Sources:

- https://www.tonomo.io/
- https://www.tonomo.io/features/booking
- https://www.hdphotohub.com/learn-best-real-estate-photography-software
- https://www.photofounder.com/blog/aryeo-vs-tonomo

## Recommended next build pass

### Before Pankaj demo

1. Use the synthetic UAT Admin account, not the legacy seed admin.
2. Show:
   - Today dashboard;
   - Tasks list;
   - delivered UAT property;
   - Secure media and approved final;
   - Availability/profile calendar ownership;
   - Integrations connected state;
   - Booking calendar intelligence;
   - Team creation/deactivate/reactivate if asked.
3. Say Xero is intentionally parked until his access is available.
4. Rotate/revoke UAT temporary credentials after the demo.

### Immediate product polish

1. Add integration sync health detail.
2. Add a guided demo mode with seeded synthetic properties and media.
3. Add richer empty-state illustration and optional warm property/media thumbnails without increasing operational noise.

### Deep production hardening after demo

1. Build real provider replay tests for Dropbox/Google/Xero webhooks.
2. Add visual regression snapshots for desktop/tablet/mobile.
3. Add accessibility scan automation beyond current static/live checks.
4. Add observability dashboard:
   - queue lag;
   - sync failures;
   - stale calendar watches;
   - token refresh failures;
   - webhook retry counts.
5. Create an operations runbook:
   - reconnect provider;
   - rotate tokens;
   - recover stuck review publish;
   - reconcile Dropbox index;
   - replay calendar handoff;
   - disable compromised user/provider connection.

## Bottom line

Northlight is no longer blocked on the shared-auth/data-plane and delivery-link issues that were preventing meaningful UAT. The product now demonstrates the intended philosophy well:

> one property, one clear workflow, one calm operational truth.

It is demo-ready with honest caveats. The final polish pass now makes actions update visibly and approved delivery feel protected. The next leap is not “more features everywhere”; it is deeper operational confidence: every integration shows health and recovery truth, every role sees only what matters, and every exception has a tested runbook.
