import{readState,error,supa,tenant}from'../../_lib/core.js';
import{storeUserOAuth}from'../../_lib/user-integrations.js';
import{startCalendarWatch}from'../../_lib/calendar-sync.js';

export async function onRequestGet({request,env}){try{const u=new URL(request.url),state=await readState(u.searchParams.get('state'),env.SESSION_SECRET),code=u.searchParams.get('code');if(!code)throw new Error(u.searchParams.get('error')||'google_code_missing');if(state.provider!=='google-user'||!state.userId)throw new Error('invalid_google_user_state');const r=await fetch('https://oauth2.googleapis.com/token',{method:'POST',headers:{'content-type':'application/x-www-form-urlencoded'},body:new URLSearchParams({code,client_id:env.GOOGLE_CLIENT_ID,client_secret:env.GOOGLE_CLIENT_SECRET,redirect_uri:`${u.origin}/oauth/google-user/callback`,grant_type:'authorization_code'})}),d=await r.json();if(!r.ok)throw new Error(JSON.stringify(d));const p=await fetch('https://www.googleapis.com/oauth2/v2/userinfo',{headers:{authorization:`Bearer ${d.access_token}`}}),me=await p.json();await storeUserOAuth(env,state.userId,'google',{...d,account_label:me.email||'Google Calendar',metadata:{email:me.email}});const t=await tenant(env);await supa(env,'provider_profiles',{method:'PATCH',query:`tenant_id=eq.${t.id}&user_id=eq.${encodeURIComponent(state.userId)}`,payload:{calendar_id:'primary'},prefer:'return=minimal'}).catch(()=>null);await startCalendarWatch(env,state.userId,'primary',u.origin);return Response.redirect(`${u.origin}/?connected=user-google`,302)}catch(e){return error(400,'Google Calendar connection failed.',e.message)}}
