import{readState,storeOAuth,error}from'../../_lib/core.js';

export async function onRequestGet({request,env}){try{const u=new URL(request.url),state=await readState(u.searchParams.get('state'),env.SESSION_SECRET),code=u.searchParams.get('code');if(state.provider!=='xero')throw new Error('invalid_xero_state');if(!code)throw new Error(u.searchParams.get('error')||'xero_code_missing');const auth=btoa(`${env.XERO_CLIENT_ID}:${env.XERO_CLIENT_SECRET}`),r=await fetch('https://identity.xero.com/connect/token',{method:'POST',headers:{authorization:`Basic ${auth}`,'content-type':'application/x-www-form-urlencoded'},body:new URLSearchParams({grant_type:'authorization_code',code,redirect_uri:`${u.origin}/oauth/xero/callback`})}),d=await r.json();if(!r.ok)throw new Error(JSON.stringify(d));const cR=await fetch('https://api.xero.com/connections',{headers:{authorization:`Bearer ${d.access_token}`,accept:'application/json'}}),connections=await cR.json();if(!cR.ok)throw new Error(JSON.stringify(connections));const c=Array.isArray(connections)?connections[0]:null;if(!c)throw new Error('No Xero organisation was connected.');await storeOAuth(env,'xero',{...d,account_label:c.tenantName||'Xero organisation',metadata:{xero_tenant_id:c.tenantId,xero_tenant_name:c.tenantName,xero_tenant_type:c.tenantType,connection_id:c.id}});return Response.redirect(`${u.origin}/?connected=xero`,302)}catch(e){return error(400,'Xero connection failed.',e.message)}}
