import{requireSession,error,json,supa,logEvent,logSync}from'../../../_lib/core.js';
import{userGoogleRequest,userIntegration}from'../../../_lib/user-integrations.js';

async function task(env,id){return(await supa(env,'tasks',{query:`select=*&id=eq.${encodeURIComponent(id)}&limit=1`}))?.[0]||null}
function canEdit(t,s){return['admin','owner'].includes(s.role)||(s.role==='agent'&&t.agent_user_id===s.userId)||(s.role==='photographer'&&t.photographer_user_id===s.userId)}
function zonedLocalToUtc(value,timeZone='Australia/Melbourne'){const local=String(value||'').slice(0,16);const[y,mo,d,h,mi]=local.match(/\d+/g)?.map(Number)||[];if(![y,mo,d,h,mi].every(Number.isFinite))return new Date(value);const guess=new Date(Date.UTC(y,mo-1,d,h,mi)),parts=new Intl.DateTimeFormat('en-US',{timeZone,year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',second:'2-digit',hourCycle:'h23'}).formatToParts(guess),p=Object.fromEntries(parts.filter(x=>x.type!=='literal').map(x=>[x.type,Number(x.value)])),shown=Date.UTC(p.year,p.month-1,p.day,p.hour,p.minute,p.second||0),offset=shown-guess.getTime();let out=new Date(guess.getTime()-offset);const parts2=new Intl.DateTimeFormat('en-US',{timeZone,year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',second:'2-digit',hourCycle:'h23'}).formatToParts(out),q=Object.fromEntries(parts2.filter(x=>x.type!=='literal').map(x=>[x.type,Number(x.value)])),shown2=Date.UTC(q.year,q.month-1,q.day,q.hour,q.minute,q.second||0),offset2=shown2-out.getTime();if(offset2!==offset)out=new Date(guess.getTime()-offset2);return out}
function legacyWallStart(iso){const d=new Date(iso);if(Number.isNaN(d.getTime()))return d;const wall=new Date(d.getTime()+10*60*60000),pad=n=>String(n).padStart(2,'0'),local=`${wall.getUTCFullYear()}-${pad(wall.getUTCMonth()+1)}-${pad(wall.getUTCDate())}T${pad(wall.getUTCHours())}:${pad(wall.getUTCMinutes())}`;return zonedLocalToUtc(local)}
export async function onRequestPatch({request,env,params}){const a=await requireSession(request,env);if(a.error)return a.error;try{const t=await task(env,params.id);if(!t||!canEdit(t,a.session))return error(404,'Task not found.');const b=await request.json(),rawStart=new Date(b.start),rawEnd=new Date(b.end||new Date(rawStart.getTime()+90*60000)),duration=Number.isNaN(rawEnd.getTime())||Number.isNaN(rawStart.getTime())?90:Math.max(1,(rawEnd-rawStart)/60000),start=b.startLocal?zonedLocalToUtc(b.startLocal):legacyWallStart(b.start),end=new Date(start.getTime()+duration*60000);if(Number.isNaN(start.getTime())||Number.isNaN(end.getTime()))return error(400,'Valid start and end times are required.');if(t.calendar_event_id&&t.calendar_owner_user_id){const ux=await userIntegration(env,t.calendar_owner_user_id,'google');if(ux?.status==='connected'){const p=(await supa(env,'provider_profiles',{query:`select=calendar_id,timezone&user_id=eq.${t.calendar_owner_user_id}&limit=1`}))?.[0]||{},calendarId=p.calendar_id||'primary',tz=p.timezone||'Australia/Melbourne';try{await userGoogleRequest(env,t.calendar_owner_user_id,`/calendar/v3/calendars/${encodeURIComponent(calendarId)}/events/${encodeURIComponent(t.calendar_event_id)}?sendUpdates=none`,{method:'PATCH',body:JSON.stringify({start:{dateTime:start.toISOString(),timeZone:tz},end:{dateTime:end.toISOString(),timeZone:tz}})});await logSync(env,'google','outbound','calendar','event_rescheduled',{entity_id:t.id,payload:{eventId:t.calendar_event_id,start:start.toISOString(),end:end.toISOString()}})}catch(e){await logSync(env,'google','outbound','calendar','reschedule_failed',{entity_id:t.id,status:'failed',error:e.message});return error(502,'Calendar could not be updated. Northlight has not changed the booking.',e.message)}}}await supa(env,'tasks',{method:'PATCH',query:`id=eq.${t.id}`,payload:{scheduled_start:start.toISOString(),scheduled_end:end.toISOString(),next_action:'Photographer should review the updated booking time.',metadata:{...(t.metadata||{}),last_schedule_change_by:a.session.userId,last_schedule_change_at:new Date().toISOString()}},prefer:'return=minimal'});await logEvent(env,{task_id:t.id,type:'schedule_changed',actor_user_id:a.session.userId,message:'Shoot schedule changed in Northlight.',detail:{from:{start:t.scheduled_start,end:t.scheduled_end},to:{start:start.toISOString(),end:end.toISOString()}}});return json({ok:true,start:start.toISOString(),end:end.toISOString()})}catch(e){return error(500,'Could not reschedule task.',e.message)}}
export async function onRequestDelete({request,env,params}){const a=await requireSession(request,env);if(a.error)return a.error;try{const t=await task(env,params.id);if(!t||!canEdit(t,a.session))return error(404,'Task not found.');if(t.calendar_event_id&&t.calendar_owner_user_id){const ux=await userIntegration(env,t.calendar_owner_user_id,'google');if(ux?.status==='connected'){const p=(await supa(env,'provider_profiles',{query:`select=calendar_id&user_id=eq.${t.calendar_owner_user_id}&limit=1`}))?.[0]||{},calendarId=p.calendar_id||'primary';try{await userGoogleRequest(env,t.calendar_owner_user_id,`/calendar/v3/calendars/${encodeURIComponent(calendarId)}/events/${encodeURIComponent(t.calendar_event_id)}?sendUpdates=none`,{method:'DELETE'})}catch(e){if(!String(e.message).includes('google_410')&&!String(e.message).includes('google_404')){await logSync(env,'google','outbound','calendar','cancel_failed',{entity_id:t.id,status:'failed',error:e.message});return error(502,'Calendar could not be cancelled. Northlight has not cancelled the task.',e.message)}}}}await supa(env,'tasks',{method:'PATCH',query:`id=eq.${t.id}`,payload:{status:'cancelled',next_action:'No further action.',metadata:{...(t.metadata||{}),cancelled_by:a.session.userId,cancelled_at:new Date().toISOString()}},prefer:'return=minimal'});await logEvent(env,{task_id:t.id,type:'task_cancelled',actor_user_id:a.session.userId,message:'Task cancelled in Northlight.'});return json({ok:true})}catch(e){return error(500,'Could not cancel task.',e.message)}}
