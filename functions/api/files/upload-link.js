import{requireSession,error,json,dropboxRequest}from'../../_lib/core.js';
import{loadTask,validateUploadRequest,LARGE_UPLOAD_THRESHOLD}from'../../_lib/media-upload.js';
import{findOrCreateDirectIntent}from'../../_lib/direct-upload.js';

export async function onRequestPost({request,env}){const a=await requireSession(request,env);if(a.error)return a.error;try{const b=await request.json(),t=await loadTask(env,b.taskId),v=validateUploadRequest(t,a.session,{stage:b.stage,serviceCode:b.serviceCode,filename:b.filename,sizeBytes:b.sizeBytes});if(!v.ok)return error(v.status,v.error);if(v.sizeBytes>LARGE_UPLOAD_THRESHOLD)return error(413,'This file needs Northlight large-file upload mode.');const x=await findOrCreateDirectIntent(env,a.session,t,v,{mimeType:b.mimeType,fingerprint:b.clientFingerprint}),row=x.row;if(!row?.id)return error(500,'Could not track this secure upload.');if(x.providerComplete)return json({url:`/api/files/direct-upload/${encodeURIComponent(row.id)}/browser`,path:row.path,stage:v.stage,serviceCode:v.service||null,expiresIn:3600,uploadIntentId:row.id,providerComplete:true,indexed:row.status==='done'});const d=await dropboxRequest(env,'files/get_temporary_upload_link',{commit_info:{path:row.path,mode:'add',autorename:false,mute:false,strict_conflict:true},duration:3600});return json({url:d.link,path:row.path,stage:v.stage,serviceCode:v.service||null,expiresIn:3600,uploadIntentId:row.id,providerComplete:false,indexed:false})}catch{return error(500,'Could not prepare secure upload.')}}
