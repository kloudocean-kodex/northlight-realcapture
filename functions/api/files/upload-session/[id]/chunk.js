import{requireSession,error,json,supa}from'../../../../_lib/core.js';
import{loadUploadSession,appendDropboxSession,UPLOAD_CHUNK_SIZE}from'../../../../_lib/media-upload.js';

export async function onRequestPost({request,env,params}){const a=await requireSession(request,env);if(a.error)return a.error;try{const x=await loadUploadSession(env,params.id,a.session);if(!x)return error(404,'Upload session not found.');const{row}=x;if(row.status!=='uploading')return error(409,'This upload session is no longer active.');if(new Date(row.expires_at)<=new Date())return error(410,'This large-file upload session expired. Start the upload again.');const offset=Number(new URL(request.url).searchParams.get('offset'));if(!Number.isSafeInteger(offset)||offset<0||offset!==Number(row.uploaded_bytes||0))return error(409,'Upload offset changed. Restart this file upload so Northlight can verify every chunk.');const bytes=new Uint8Array(await request.arrayBuffer());if(!bytes.byteLength)return error(400,'The upload chunk is empty.');if(bytes.byteLength>UPLOAD_CHUNK_SIZE)return error(413,'Upload chunk is larger than Northlight allows.');if(offset+bytes.byteLength>Number(row.size_bytes))return error(409,'Upload data exceeds the selected file size.');await appendDropboxSession(env,row.dropbox_session_id,offset,bytes);const uploaded=offset+bytes.byteLength;await supa(env,'media_upload_sessions',{method:'PATCH',query:`id=eq.${encodeURIComponent(row.id)}`,payload:{uploaded_bytes:uploaded,updated_at:new Date().toISOString(),last_error:null},prefer:'return=minimal'});return json({ok:true,uploadedBytes:uploaded,sizeBytes:Number(row.size_bytes),complete:uploaded===Number(row.size_bytes)})}catch{return error(502,'Dropbox could not accept this upload chunk. Northlight kept the confirmed offset so it can be retried safely.')}}
