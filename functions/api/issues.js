import{requireSession,error,json,supa,tenant,logEvent,issueEmail}from'../_lib/core.js';

function canTask(t,s){return ['admin','owner'].includes(s.role)||(s.role==='agent'&&t.agent_user_id===s.userId)||(s.role==='photographer'&&t.photographer_user_id===s.userId)||(s.role==='editor'&&t.editor_user_id===s.userId)}
async function task(env,id){return(await supa(env,'tasks',{query:`select=*&id=eq.${encodeURIComponent(id)}&limit=1`}))?.[0]||null}

export async function onRequestGet({request,env}){const a=await requireSession(request,env);if(a.error)return a.error;try{const u=new URL(request.url),taskId=u.searchParams.get('taskId');if(taskId){const t=await task(env,taskId);if(!t||!canTask(t,a.session))return error(404,'Task not found.');const rows=await supa(env,'task_issues',{query:`select=*,creator:users!task_issues_created_by_user_id_fkey(name),assignee:users!task_issues_assigned_to_user_id_fkey(name)&task_id=eq.${taskId}&order=created_at.desc`});return json({issues:rows})}if(!['admin','owner'].includes(a.session.role))return error(403,'permission_denied');const rows=await supa(env,'task_issues',{query:'select=*&order=created_at.desc&limit=100'});return json({issues:rows})}catch(e){return error(500,'Could not load issues.',e.message)}}

export async function onRequestPost({request,env}){const a=await requireSession(request,env);if(a.error)return a.error;try{const b=await request.json(),t=await task(env,b.taskId);if(!t||!canTask(t,a.session))return error(404,'Task not found.');const tn=await tenant(env),rows=await supa(env,'task_issues',{method:'POST',payload:{tenant_id:tn.id,task_id:t.id,created_by_user_id:a.session.userId,assigned_to_user_id:b.assignedToUserId||null,subject:String(b.subject||'').trim(),description:String(b.description||'').trim(),category:b.category||'general',priority:b.priority||'normal',status:'open',due_at:b.dueAt||null}});await logEvent(env,{task_id:t.id,type:'issue_raised',actor_user_id:a.session.userId,message:`Issue raised: ${b.subject}`,detail:{priority:b.priority||'normal',category:b.category||'general'}});try{await issueEmail(env,`${t.task_no} · Issue raised: ${b.subject}`,`${a.session.name} raised an issue on ${t.property_name}.\n\n${b.description||''}\n\nOpen Northlight to review it.`)}catch{}return json({issue:rows[0]})}catch(e){return error(500,'Could not raise issue.',e.message)}}
