const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
const fmt=d=>d?new Intl.DateTimeFormat('en-AU',{weekday:'short',day:'numeric',month:'short',hour:'numeric',minute:'2-digit',timeZone:'Australia/Melbourne'}).format(new Date(d)):'—';
const dateKey=d=>d?new Intl.DateTimeFormat('en-CA',{year:'numeric',month:'2-digit',day:'2-digit',timeZone:'Australia/Melbourne'}).format(new Date(d)):'—';
const ico=(n,c='nl-ico')=>window.NLIcon?window.NLIcon(n,c):'';
const state={session:null,bootstrap:null,view:'today',tasks:[],selected:null,wizard:null};
const labels={today:'Today',tasks:'Tasks',booking:'Booking',attention:'Needs attention',team:'Team',availability:'Availability',editor:'Editor queue',services:'Services & packages',roles:'Roles & access',integrations:'Integrations',settings:'Settings'};
const navIcons={today:'home',tasks:'tasks',booking:'calendar',attention:'alert',team:'team',availability:'clock',editor:'editor',services:'plus',roles:'shield',integrations:'link',settings:'settings'};
const serviceIcons={photos:'camera',floorplan:'floorplan',video:'video',drone:'drone',twilight:'twilight',reels:'social'};
async function api(path,opt={}){const r=await fetch(path,{credentials:'include',headers:{'content-type':'application/json',...(opt.headers||{})},...opt});let d={};try{d=await r.json()}catch{}if(!r.ok){const detail=typeof d.detail==='string'&&d.detail.length<180?` · ${d.detail}`:'';throw new Error((d.error||`Request failed (${r.status})`)+detail)}return d}
function toast(title,msg=''){const t=$('#toast');if(!t)return;t.innerHTML=`<strong>${esc(title)}</strong>${msg?`<span>${esc(msg)}</span>`:''}`;t.classList.remove('hidden');clearTimeout(window.__nlToast);window.__nlToast=setTimeout(()=>t.classList.add('hidden'),3800)}
function initials(n='Northlight'){return String(n).split(/\s+/).filter(Boolean).map(x=>x[0]).slice(0,2).join('').toUpperCase()||'N'}
function navFor(role){if(role==='admin')return['today','tasks','booking','attention','team','availability','editor','services','roles','integrations','settings'];if(role==='owner')return['today','tasks','booking','attention','team','availability','integrations'];if(role==='agent')return['today','tasks','booking','attention'];if(role==='photographer')return['today','tasks','availability'];if(role==='editor')return['today','tasks','editor'];return['today','tasks']}
function person(id){return(state.bootstrap?.users||[]).find(x=>x.id===id)}
function serviceName(code){return(state.bootstrap?.services||[]).find(x=>x.code===code)?.name||String(code||'').replaceAll('_',' ')}
function statusBadge(s){const v=String(s||'assigned').replaceAll('_',' '),bad=['declined','cancelled','failed'].includes(s),warn=['assigned','revision','reschedule_requested','waiting'].includes(s);return`<span class="badge ${bad?'bad':warn?'warn':''}"><i class="dot"></i>${esc(v)}</span>`}
function greeting(){const h=Number(new Intl.DateTimeFormat('en-AU',{hour:'2-digit',hour12:false,timeZone:'Australia/Melbourne'}).format(new Date()));return h<12?'Good morning':h<18?'Good afternoon':'Good evening'}
function roleCanCreate(){return['admin','owner','agent'].includes(state.session?.role)}
function visibleTasks(){return state.tasks||[]}
function loginView(){return`<div class="login"><section class="login-art"><div class="mark"><div class="logo">N</div>Northlight</div><div class="login-copy"><div class="eyebrow">REALCAPTURE · Property media operations</div><h1>One property.<br>One clear workflow.</h1><p>Book. Shoot. Edit. Deliver.<br><strong>Everyone knows what happens next.</strong></p></div><small>Melbourne · Australia</small></section><section class="login-form-wrap"><form class="login-card" id="loginForm"><div class="eyebrow">Secure workspace</div><h2>Sign in</h2><p>Use the credentials provided by your administrator.</p><div class="field"><label>Email</label><input id="email" type="email" autocomplete="username" required placeholder="you@company.com"></div><div class="field"><label>Password</label><input id="password" type="password" autocomplete="current-password" required placeholder="••••••••••"></div><button class="btn primary block">${ico('lock')}Sign in</button><div id="loginError" class="hint hidden"></div><div class="crafted">${ico('spark')}<span>Crafted by <strong>ProddyG</strong></span></div></form></section></div>`}
function appFrame(){const n=navFor(state.session.role);return`<div class="app"><aside class="sidebar"><div class="mark"><div class="logo">N</div>Northlight</div><nav class="nav">${n.map(x=>`<button data-view="${x}" class="${state.view===x?'active':''}"><span>${ico(navIcons[x])}</span>${labels[x]}</button>`).join('')}<div class="section">Quick action</div>${roleCanCreate()?`<button data-new-task><span>${ico('plus')}</span>New property task</button>`:''}</nav><div class="sidebar-user"><div class="avatar">${initials(state.session.name)}</div><div><strong>${esc(state.session.name)}</strong><span>${esc(state.session.roleLabel||state.session.role)}</span></div></div><div class="crafted sidebar-crafted">${ico('spark')}<span>Crafted by <strong>ProddyG</strong></span></div></aside><main class="main"><header class="topbar"><div><h2>${labels[state.view]||'Northlight'}</h2><small>REALCAPTURE · ${esc(state.session.name)}</small></div><div class="toolbar"><span class="role-pill">${esc(state.session.roleLabel||state.session.role)}</span><button class="btn secondary" id="logoutBtn">${ico('logout')}Sign out</button></div></header><section class="content" id="page"></section></main><nav class="mobile-nav">${['today','tasks','booking','attention'].filter(x=>n.includes(x)).map(x=>`<button data-view="${x}">${ico(navIcons[x])}<br>${labels[x]}</button>`).join('')}</nav></div>`}
function taskRow(t){const ph=person(t.photographer_user_id);return`<button class="row task-row" data-task="${t.id}"><div><strong>${esc(t.property_name)}</strong><small>${esc(t.address)} · ${esc(t.suburb)} · ${esc(t.task_no)}</small></div><div><strong>${esc(ph?.name||'Unassigned')}</strong><small>Photographer</small></div><div><strong>${fmt(t.scheduled_start)}</strong><small>${(t.service_codes||[]).map(serviceName).join(' · ')}</small></div>${statusBadge(t.status)}</button>`}
function attentionItems(){const a=[];for(const t of visibleTasks()){if(t.status==='assigned')a.push([t,'Photographer confirmation',`${person(t.photographer_user_id)?.name||'Photographer'} has not confirmed yet.`]);if(t.status==='declined')a.push([t,'Photographer declined','Assign another suitable photographer.']);if(t.status==='reschedule_requested')a.push([t,'New time requested','Review the proposed appointment time.']);if(t.status==='review')a.push([t,'Review ready','Edited media is waiting for approval.']);if(t.status==='raw_received'&&!t.editor_user_id)a.push([t,'Editor required','Source media is ready but no editor is assigned.']);if(t.priority==='rush'&&!['delivered','cancelled'].includes(t.status))a.push([t,'Rush task',`Keep ${t.task_no} moving against its priority SLA.`])}return a}
function ownerTeamPulse(){const users=state.bootstrap?.users||[],cards=users.filter(x=>['agent','photographer','editor'].includes(x.role_code)).slice(0,8);return`<div class="card owner-card"><div class="card-head"><h3>Team pulse</h3><button class="btn ghost" data-view="team">Full team view</button></div><div class="card-body team-board">${cards.map(u=>{const active=state.tasks.filter(t=>!['delivered','cancelled'].includes(t.status)&&[t.agent_user_id,t.photographer_user_id,t.editor_user_id].includes(u.id)).length;return`<button class="person person-button" data-person="${u.id}"><div class="person-top"><div class="avatar">${initials(u.name)}</div><div><h4>${esc(u.name)}</h4><p>${esc(u.role_code)} · ${active} active</p></div></div><div class="meter"><i style="width:${Math.min(100,active*18+10)}%"></i></div></button>`}).join('')}</div></div>`}
function todayView(){const ts=visibleTasks(),today=dateKey(new Date()),todayJobs=ts.filter(t=>dateKey(t.scheduled_start)===today),attention=attentionItems(),delivered=ts.filter(t=>t.status==='delivered').length,owner=['admin','owner'].includes(state.session.role);return`<div class="hero"><div><div class="eyebrow">Operations at a glance</div><h1>${greeting()}, ${esc(state.session.name.split(' ')[0])}.</h1><p>Routine work stays quiet. Decisions, exceptions and hand-offs come to you.</p></div>${roleCanCreate()?`<button class="btn primary" data-new-task>${ico('plus')}New property task</button>`:''}</div><div class="grid kpis"><div class="card kpi"><span>Active tasks</span><strong>${ts.filter(t=>!['delivered','cancelled'].includes(t.status)).length}</strong></div><div class="card kpi"><span>Today</span><strong>${todayJobs.length}</strong></div><div class="card kpi"><span>Needs attention</span><strong>${attention.length}</strong></div><div class="card kpi"><span>Delivered</span><strong>${delivered}</strong></div></div><div class="columns"><div class="card"><div class="card-head"><h3>Current work</h3><button class="btn ghost" data-view="tasks">View all</button></div><div class="list">${ts.slice(0,6).map(taskRow).join('')||'<div class="empty">No tasks yet. Create the first property task.</div>'}</div></div><div class="card"><div class="card-head"><h3>Needs attention</h3><span class="badge ${attention.length?'warn':''}">${attention.length}</span></div><div class="card-body attention">${attention.slice(0,5).map(([t,h,p])=>`<button class="attention-item" data-task="${t.id}"><strong>${esc(h)} · ${esc(t.property_name)}</strong><p>${esc(p)}</p></button>`).join('')||'<div class="empty">Nothing needs intervention.</div>'}</div></div></div>${owner?ownerTeamPulse()+`<div id="opsCard" class="card owner-card"><div class="card-head"><h3>${ico('route')}Live operations timeline</h3><span class="badge">Loading</span></div><div class="empty">Loading the operational audit stream…</div></div><div id="financeCard" class="card owner-card"><div class="card-head"><h3>${ico('invoice')}Finance snapshot</h3><span class="badge">Owner only</span></div><div class="empty">Loading finance visibility…</div></div>`:''}`}
function tasksView(){const ts=visibleTasks();return`<div class="hero"><div><div class="eyebrow">One property · one record</div><h1>Tasks.</h1><p>Booking, files, editing, comments, issues and delivery remain attached to the same property.</p></div>${roleCanCreate()?`<button class="btn primary" data-new-task>${ico('plus')}New property task</button>`:''}</div><div class="card"><div class="list">${ts.map(taskRow).join('')||'<div class="empty">No visible tasks.</div>'}</div></div>`}
function attentionView(){const a=attentionItems();return`<div class="hero"><div><div class="eyebrow">Exception-first operations</div><h1>Needs attention.</h1><p>Only work requiring a decision, recovery or review belongs here.</p></div></div><div class="card"><div class="card-body attention">${a.map(([t,h,p])=>`<button class="attention-item" data-task="${t.id}"><strong>${esc(h)} · ${esc(t.property_name)}</strong><p>${esc(p)}</p></button>`).join('')||'<div class="empty">Everything is moving normally.</div>'}</div></div>`}
function teamView(){const users=state.bootstrap?.users||[];return`<div class="hero"><div><div class="eyebrow">Owner visibility</div><h1>Team.</h1><p>Understand workload, coverage and connected calendars without exposing controls people do not need.</p></div>${state.session.role==='admin'?`<button class="btn primary" id="addTeam">${ico('plus')}Add team member</button>`:''}</div><div class="team-board">${users.filter(x=>x.active!==false).map(u=>{const assigned=state.tasks.filter(t=>!['delivered','cancelled'].includes(t.status)&&[t.agent_user_id,t.photographer_user_id,t.editor_user_id].includes(u.id));return`<button class="card person person-button" data-person="${u.id}"><div class="person-top"><div class="avatar">${initials(u.name)}</div><div><h4>${esc(u.name)}</h4><p>${esc(u.role_code)} · ${esc(u.email)}</p></div></div><div class="meter"><i style="width:${Math.min(100,assigned.length*17+8)}%"></i></div><p style="margin-top:12px">${assigned.length} active assignment${assigned.length===1?'':'s'} · View details →</p></button>`}).join('')}</div>`}
function nextBusinessDays(n=5){const out=[],d=new Date();while(out.length<n){d.setDate(d.getDate()+1);const day=new Intl.DateTimeFormat('en-AU',{weekday:'short',timeZone:'Australia/Melbourne'}).format(d);if(!['Sat','Sun'].includes(day))out.push(new Date(d))}return out}
function bookingView(){const days=nextBusinessDays(),active=visibleTasks().filter(t=>!['cancelled','delivered'].includes(t.status));return`<div class="hero"><div><div class="eyebrow">Scheduling intelligence</div><h1>Booking.</h1><p>Area, services, working hours and the selected photographer’s connected calendar are validated before assignment.</p></div>${roleCanCreate()?`<button class="btn primary" data-new-task>${ico('calendar')}Book a property</button>`:''}</div><div class="schedule-days">${days.map(d=>{const k=dateKey(d),jobs=active.filter(t=>dateKey(t.scheduled_start)===k);return`<div class="card schedule-day"><div class="eyebrow">${new Intl.DateTimeFormat('en-AU',{weekday:'short',day:'numeric',month:'short',timeZone:'Australia/Melbourne'}).format(d)}</div>${jobs.length?jobs.map(t=>`<button data-task="${t.id}" class="schedule-job"><strong>${new Intl.DateTimeFormat('en-AU',{hour:'numeric',minute:'2-digit',timeZone:'Australia/Melbourne'}).format(new Date(t.scheduled_start))}</strong><span>${esc(t.property_name)}</span><small>${esc(person(t.photographer_user_id)?.name||'Unassigned')}</small></button>`).join(''):'<div class="schedule-empty">No Northlight bookings</div>'}</div>`}).join('')}</div><div class="hint" style="margin-top:18px">This board shows Northlight bookings. Final availability is validated against the selected photographer’s working rules and connected Google Calendar during booking.</div>`}
function availabilityView(){const profiles=state.bootstrap?.providers||[],users=state.bootstrap?.users||[],own=state.session.role==='photographer';if(own){const p=profiles.find(x=>x.user_id===state.session.userId);return`<div class="hero"><div><div class="eyebrow">Your availability</div><h1>Availability.</h1><p>Your working rules and Google Calendar determine when Northlight can book you.</p></div><button class="btn primary" id="connectMyCalendar">${ico('calendar')}Connect / refresh Google Calendar</button></div>${providerCard(p,person(p?.user_id),true)}<div id="myCalStatus" class="hint">Checking connected calendar…</div>`}return`<div class="hero"><div><div class="eyebrow">Photographer-controlled availability</div><h1>Availability.</h1><p>Working hours, coverage, travel buffer and personal Calendar connections combine into bookable capacity.</p></div></div><div id="calendarCoverage" class="grid provider-grid">${profiles.map(p=>providerCard(p,users.find(x=>x.id===p.user_id),false)).join('')}</div>`}
function providerCard(p,u,own){if(!p)return'<div class="card section-card"><div class="empty">No photographer profile configured.</div></div>';return`<div class="card section-card ${own?'wide-card':''}" ${!own?`data-person="${p.user_id}"`:''}><div class="person-top"><div class="avatar">${initials(u?.name)}</div><div><h3 style="margin:0">${esc(u?.name)}</h3><p style="margin:4px 0;color:var(--muted)">${esc((p.areas||[]).join(' · '))}</p></div></div><div class="mini-badges"><span class="badge">${p.service_radius_km||25} km radius</span><span class="badge">${p.travel_buffer_min||15} min travel buffer</span></div><p class="detail-copy">${ico('files')} ${(p.service_codes||[]).map(serviceName).join(' · ')}<br>${ico('route')} ${esc(p.home_base||'Melbourne')}<br>${ico('clock')} ${esc(p.timezone||'Australia/Melbourne')}</p><div class="calendar-chip" data-calendar-user="${p.user_id}">Calendar status loading…</div></div>`}
function editorView(){const ts=visibleTasks().filter(t=>['raw_received','editing','revision'].includes(t.status));return`<div class="hero"><div><div class="eyebrow">Post-production queue</div><h1>Editor queue.</h1><p>Source media, active work and revisions stay attached to the property record.</p></div></div><div class="card"><div class="list">${ts.map(taskRow).join('')||'<div class="empty">No editing jobs in queue.</div>'}</div></div>`}
function servicesView(){const sv=state.bootstrap?.services||[],pk=state.bootstrap?.packages||[];return`<div class="hero"><div><div class="eyebrow">Booking catalogue</div><h1>Services & packages.</h1><p>Duration and buffers drive real booking calculations; packages keep repeat orders fast.</p></div></div><div class="columns"><div class="card"><div class="card-head"><h3>Services</h3></div><div class="service-admin-grid">${sv.map(x=>`<div class="service-admin"><span class="service-icon">${ico(serviceIcons[x.code]||'spark')}</span><div><strong>${esc(x.name)}</strong><small>${x.duration_min} min · ${x.buffer_before_min+x.buffer_after_min} min buffer</small></div><span class="badge">Active</span></div>`).join('')}</div></div><div class="card"><div class="card-head"><h3>Saved packages</h3></div><div class="card-body">${pk.map(x=>`<div class="attention-item"><strong>${esc(x.name)}</strong><p>${(x.service_codes||[]).map(serviceName).join(' · ')}</p></div>`).join('')||'<div class="empty">No saved packages.</div>'}</div></div></div>`}
function rolesView(){const roles=state.bootstrap?.roles||[];return`<div class="hero"><div><div class="eyebrow">Role-based control</div><h1>Roles & access.</h1><p>Permissions are enforced at the API and file-link level—not simply hidden in the interface.</p></div></div><div class="grid role-grid">${roles.map(r=>`<div class="card section-card"><span class="service-icon">${ico('shield')}</span><span class="badge">${esc(r.code)}</span><h3>${esc(r.name)}</h3><p class="detail-copy">${(r.permissions||[]).map(esc).join(' · ')||'Configured permissions'}</p></div>`).join('')}</div>`}
function integrationsView(){const s=state.bootstrap?.integrations||[],get=p=>s.find(x=>x.provider===p)||{},card=(p,title,icon,desc,actions)=>{const x=get(p),connected=x.status==='connected',disabled=x.status==='disabled';return`<div class="card integration"><div class="integration-title"><span class="provider-icon">${ico(icon)}</span><div><h3>${title}</h3>${x.account_label?`<small>${esc(x.account_label)}</small>`:''}</div><span class="badge status ${disabled?'warn':''}">${disabled?'Disabled':connected?'Connected':'Not connected'}</span></div><p>${desc}</p><div class="integration-actions">${actions}</div></div>`};return`<div class="hero"><div><div class="eyebrow">Connected services</div><h1>Integrations.</h1><p>Northlight coordinates the operation while specialist systems remain the systems of record.</p></div></div><div class="integration-grid">${card('dropbox','Dropbox','files','Private RAW, edited and final media. Northlight indexes external changes and controls role-safe access.',`<button class="btn primary" data-connect="dropbox">${get('dropbox').status==='connected'?'Reconnect':'Connect Dropbox'}</button><button class="btn secondary" data-test="dropbox">Verify</button>`)}${card('google','Google Workspace','mail','Shared email plus photographer-owned calendars, conflict checks and two-way schedule history.',`<button class="btn primary" data-connect="google">${get('google').status==='connected'?'Reconnect Gmail':'Connect Gmail'}</button><button class="btn secondary" data-test="google">Verify</button>`)}${card('xero','Xero','invoice','Optional accounting connection for draft invoices and payment-status visibility to Owner/Admin only.',`<button class="btn primary" id="xeroConnect" data-connect="xero">Connect Xero</button><button class="btn secondary" data-test="xero">Verify</button>`)}${card('whatsapp','WhatsApp','message','Optional channel intentionally disabled for the current REALCAPTURE pilot. Email is active.','<button class="btn secondary" disabled>Disabled</button>')}</div><div id="integrationPreflight" class="hint">Checking cloud configuration…</div>`}
function settingsView(){return`<div class="hero"><div><div class="eyebrow">Tenant configuration</div><h1>Settings.</h1><p>Northlight is currently configured for REALCAPTURE and Australia/Melbourne.</p></div></div><div class="grid settings-grid"><div class="card section-card"><span class="service-icon">${ico('shield')}</span><h3>Workspace</h3><p class="detail-copy">Northlight · REALCAPTURE<br>Australia/Melbourne<br>Cloudflare Pages + Supabase Sydney</p></div><div class="card section-card"><span class="service-icon">${ico('mail')}</span><h3>Communication</h3><p class="detail-copy">Email enabled<br>WhatsApp disabled for pilot<br>Task comments remain the source of truth</p></div><div class="card section-card"><span class="service-icon">${ico('files')}</span><h3>Media</h3><p class="detail-copy">Dropbox App Folder<br>Role-checked temporary links<br>No RAW media stored in Northlight</p></div></div>`}
function renderPage(){const p=$('#page');if(!p)return;const map={today:todayView,tasks:tasksView,booking:bookingView,attention:attentionView,team:teamView,availability:availabilityView,editor:editorView,services:servicesView,roles:rolesView,integrations:integrationsView,settings:settingsView};p.innerHTML=(map[state.view]||todayView)();bindDynamic();hydratePage()}
function render(){const root=$('#root');if(!state.session){root.innerHTML=loginView();bindLogin();return}root.innerHTML=appFrame();renderPage();$('#logoutBtn').onclick=logout}
function bindDynamic(){$$('[data-task]').forEach(b=>b.onclick=()=>openTask(b.dataset.task));$$('[data-person]').forEach(b=>b.onclick=()=>openPerson(b.dataset.person));$$('[data-view]').forEach(b=>b.onclick=()=>{state.view=b.dataset.view;render()});$$('[data-new-task]').forEach(b=>b.onclick=openWizard);$$('[data-connect]').forEach(b=>b.onclick=()=>connect(b.dataset.connect));$$('[data-test]').forEach(b=>b.onclick=()=>testIntegration(b.dataset.test));if($('#addTeam'))$('#addTeam').onclick=openAddTeam;if($('#connectMyCalendar'))$('#connectMyCalendar').onclick=()=>location.href='/api/calendar/connect'}
async function hydratePage(){if(state.view==='today'&&['admin','owner'].includes(state.session.role))hydrateOwnerToday();if(state.view==='availability')hydrateCalendars();if(state.view==='integrations')hydrateIntegrations()}
async function hydrateOwnerToday(){try{const d=await api('/api/operations/activity'),box=$('#opsCard');if(box)box.innerHTML=`<div class="card-head"><h3>${ico('route')}Live operations timeline</h3><div><span class="badge">${d.issues?.length||0} open issues</span> <span class="badge">${d.invoices?.length||0} invoices</span></div></div><div class="ops-list">${(d.activity||[]).slice(0,10).map(x=>`<div class="ops-item"><span class="source-mark">${ico(x.source==='dropbox'?'files':x.source==='google'?'calendar':x.source==='xero'?'invoice':'route')}</span><div><strong>${esc(x.message)}</strong><small>${x.task?esc(`${x.task.task_no} · ${x.task.property_name}`):esc(x.source)} · ${fmt(x.at)}</small></div></div>`).join('')||'<div class="empty">Activity will appear as the team works.</div>'}</div>`}catch(e){const box=$('#opsCard');if(box)box.innerHTML=`<div class="card-head"><h3>Live operations timeline</h3></div><div class="hint">${esc(e.message)}</div>`}try{const d=await api('/api/finance/invoices'),rows=d.invoices||[],total=rows.reduce((n,x)=>n+Number(x.total||0),0),paid=rows.filter(x=>['paid','PAID'].includes(x.status)).reduce((n,x)=>n+Number(x.total||0),0),out=total-paid,box=$('#financeCard');if(box)box.innerHTML=`<div class="card-head"><h3>${ico('invoice')}Finance snapshot</h3><span class="badge">Owner only</span></div><div class="finance-grid"><div><small>Invoices</small><strong>${rows.length}</strong></div><div><small>Total</small><strong>A$${total.toLocaleString('en-AU')}</strong></div><div><small>Outstanding</small><strong>A$${out.toLocaleString('en-AU')}</strong></div><div><small>Paid</small><strong>A$${paid.toLocaleString('en-AU')}</strong></div></div>`}catch(e){const box=$('#financeCard');if(box)box.innerHTML=`<div class="card-head"><h3>Finance snapshot</h3></div><div class="hint">${esc(e.message)}</div>`}}
async function hydrateCalendars(){if(state.session.role==='photographer'){try{const d=await api('/api/calendar/status'),b=$('#myCalStatus');if(b)b.innerHTML=d.connected?`<strong>Connected</strong> · ${esc(d.account||'Google Calendar')}${d.sync?.last_error?`<br><span class="warn-text">${esc(d.sync.last_error)}</span>`:''}`:'<strong>Not connected.</strong> Connect Google Calendar to protect external busy time.'}catch(e){const b=$('#myCalStatus');if(b)b.textContent=e.message}return}if(!['admin','owner'].includes(state.session.role))return;try{const d=await api('/api/operations/calendars');for(const row of d.calendars||[]){const el=$(`[data-calendar-user="${row.user.id}"]`);if(el)el.innerHTML=row.integration?.status==='connected'?`${ico('check')} <strong>Calendar connected</strong> · ${esc(row.integration.account_label||'Google')}`:`${ico('alert')} <strong>Calendar not connected</strong>`}}catch{}}
async function hydrateIntegrations(){try{const d=await api('/api/preflight'),box=$('#integrationPreflight');if(box){const missing=d.missing||[];box.innerHTML=d.ok?'<strong>Core cloud configuration is ready.</strong> Optional integrations can be connected independently.':`<strong>Core configuration needs attention:</strong> ${esc(missing.join(', '))}`;const xeroReady=d.configured?.XERO_CLIENT_ID&&d.configured?.XERO_CLIENT_SECRET,xb=$('#xeroConnect');if(xb&&!xeroReady){xb.disabled=true;xb.textContent='Setup pending'}}}catch(e){const box=$('#integrationPreflight');if(box)box.textContent=e.message}}
function bindLogin(){$('#loginForm').onsubmit=async e=>{e.preventDefault();const box=$('#loginError'),btn=$('#loginForm button[type="submit"],#loginForm button');box.classList.add('hidden');if(btn)btn.disabled=true;try{const d=await api('/api/auth/login',{method:'POST',body:JSON.stringify({email:$('#email').value,password:$('#password').value})});state.session=d.session;await bootstrap();render()}catch(err){box.textContent=err.message;box.classList.remove('hidden')}finally{if(btn)btn.disabled=false}}}
async function logout(){try{await api('/api/auth/logout',{method:'POST'})}catch{}state.session=null;state.bootstrap=null;state.tasks=[];render()}
async function bootstrap(){const d=await api('/api/bootstrap');state.bootstrap=d;state.tasks=d.tasks||[];if(!state.session)state.session=d.session}
function wizardSteps(){return[1,2,3,4].map(x=>`<div class="step ${state.wizard.step>=x?'on':''}"></div>`).join('')}
function agents(){return(state.bootstrap?.users||[]).filter(x=>x.active!==false&&x.role_code==='agent')}
function providerMatches(w){return(state.bootstrap?.providers||[]).map(p=>{const u=person(p.user_id),missing=(w.services||[]).filter(s=>!(p.service_codes||[]).includes(s)),area=(p.areas||[]).includes(w.area||'Inner East');return{...p,user:u,missing,area,full:area&&!missing.length}}).sort((a,b)=>Number(b.full)-Number(a.full)||a.missing.length-b.missing.length||Number(a.current_load||0)-Number(b.current_load||0))}
function openWizard(){state.wizard={step:1,services:[],priority:'standard',idempotencyKey:crypto.randomUUID(),agentId:state.session.role==='agent'?state.session.userId:agents()[0]?.id||null,suburb:'Toorak',area:'Inner East'};drawWizard()}
function saveWizard(){const w=state.wizard;if($('#wProperty'))w.property=$('#wProperty').value.trim();if($('#wAddress'))w.address=$('#wAddress').value.trim();if($('#wSuburb'))w.suburb=$('#wSuburb').value.trim();if($('#wArea')){const old=w.area;w.area=$('#wArea').value;if(old&&old!==w.area)w.photographerId=null}if($('#wAgent'))w.agentId=$('#wAgent').value;if($('#wPriority'))w.priority=$('#wPriority').value;if($('#wDate'))w.date=$('#wDate').value;if($('#wTime'))w.time=$('#wTime').value}
function drawWizard(){const w=state.wizard,sv=state.bootstrap?.services||[],matches=providerMatches(w),full=matches.filter(x=>x.full);if(w.step===3&&(!w.photographerId||!full.some(x=>x.user_id===w.photographerId)))w.photographerId=full[0]?.user_id||null;let body='';if(w.step===1)body=`<div class="form-grid"><div class="field span-2"><label>Property / task name</label><input id="wProperty" value="${esc(w.property||'')}" placeholder="24 Albany Road"></div><div class="field span-2"><label>Address</label><input id="wAddress" value="${esc(w.address||'')}" placeholder="24 Albany Road, Toorak VIC 3142"></div><div class="field"><label>Suburb</label><input id="wSuburb" value="${esc(w.suburb||'')}" placeholder="Toorak"></div><div class="field"><label>Area</label><select id="wArea">${['Inner East','Inner South','Bayside','CBD & Inner City','North','West'].map(x=>`<option ${w.area===x?'selected':''}>${x}</option>`).join('')}</select></div>${['admin','owner'].includes(state.session.role)?`<div class="field span-2"><label>Agent</label><select id="wAgent">${agents().map(x=>`<option value="${x.id}" ${w.agentId===x.id?'selected':''}>${esc(x.name)}</option>`).join('')}</select></div>`:''}</div>`;if(w.step===2)body=`<div class="service-grid">${sv.filter(x=>x.active!==false).map(x=>`<button class="service ${w.services.includes(x.code)?'on':''}" data-service="${x.code}"><span class="service-icon">${ico(serviceIcons[x.code]||'spark')}</span><strong>${esc(x.name)}</strong><small>${x.duration_min} min · ${x.buffer_before_min+x.buffer_after_min} min buffer</small></button>`).join('')}</div><div class="field" style="margin-top:18px"><label>Priority</label><select id="wPriority"><option value="standard" ${w.priority==='standard'?'selected':''}>Standard</option><option value="priority" ${w.priority==='priority'?'selected':''}>Priority</option><option value="rush" ${w.priority==='rush'?'selected':''}>Rush</option></select></div>`;if(w.step===3)body=`${full.length?'<div class="match-note good">'+ico('check')+' Full matches are based on area + every selected service. Calendar conflicts are checked next.</div>':'<div class="match-note warn">'+ico('alert')+' No single photographer covers this exact area + service combination. Go back and adjust the area/services, or configure photographer coverage in Admin.</div>'}<div class="photo-choice">${matches.map((p,i)=>`<button class="photo-option ${p.full&&w.photographerId===p.user_id?'on':''} ${!p.full?'partial':''}" data-provider="${p.user_id}" ${!p.full?'disabled':''}><div class="top"><strong>${esc(p.user?.name||'Photographer')}</strong>${p.full&&i===0?'<span class="badge">Recommended</span>':p.full?'<span class="badge">Full match</span>':'<span class="badge warn">Partial</span>'}</div><small>${p.area?'Area covered':'Area not covered'} · ${(p.service_codes||[]).map(serviceName).join(' · ')}</small>${p.missing.length?`<small class="missing">Missing: ${p.missing.map(serviceName).join(', ')}</small>`:''}</button>`}).join('')}</div>`;if(w.step===4){const ph=person(w.photographerId);body=`<div class="selected-provider">${ico('camera')}<div><strong>${esc(ph?.name||'Photographer')}</strong><small>${(w.services||[]).map(serviceName).join(' · ')}</small></div></div><div class="form-grid"><div class="field"><label>Date</label><input id="wDate" type="date" value="${w.date||new Date(Date.now()+86400000).toISOString().slice(0,10)}"></div><div class="field"><label>Preferred time</label><select id="wTime">${['07:30','08:00','09:00','10:30','12:00','13:30','15:00','16:30'].map(x=>`<option ${w.time===x?'selected':''}>${x}</option>`).join('')}</select></div></div><div id="slotCheck" class="hint">Checking photographer availability…</div>`}const canContinue=w.step!==3||!!w.photographerId;$('#overlay').innerHTML=`<div class="modal-bg"><div class="modal"><div class="modal-head"><div><div class="eyebrow">New property task · Step ${w.step} of 4</div><h2>${['','Property','Services','Photographer','Schedule'][w.step]}</h2></div><button class="btn secondary icon-btn" id="closeModal">×</button></div><div class="modal-body"><div class="steps">${wizardSteps()}</div>${body}<div class="toolbar wizard-actions">${w.step>1?'<button class="btn secondary" id="backStep">Back</button>':''}<button class="btn primary" id="nextStep" ${!canContinue||w.submitting?'disabled':''}>${w.submitting?'Creating…':w.step===4?'Create & assign':'Continue'}</button></div></div></div></div>`;$('#closeModal').onclick=closeOverlay;if($('#backStep'))$('#backStep').onclick=()=>{saveWizard();w.step--;drawWizard()};$$('[data-service]').forEach(b=>b.onclick=()=>{const c=b.dataset.service;w.services=w.services.includes(c)?w.services.filter(x=>x!==c):[...w.services,c];w.photographerId=null;drawWizard()});$$('[data-provider]').forEach(b=>b.onclick=()=>{if(!b.disabled){w.photographerId=b.dataset.provider;drawWizard()}});$('#nextStep').onclick=async()=>{saveWizard();if(w.step===1&&(!w.property||!w.address))return toast('Property details required','Add a property name and address.');if(w.step===2&&!w.services.length)return toast('Choose at least one service');if(w.step===3&&!w.photographerId)return toast('No full-match photographer','Adjust the area or services before continuing.');if(w.step<4){w.step++;drawWizard();return}await createTask()};if(w.step===4){$('#wDate').onchange=()=>{saveWizard();checkSlot()};$('#wTime').onchange=()=>{saveWizard();checkSlot()};checkSlot()}}
async function checkSlot(){const w=state.wizard,box=$('#slotCheck');if(!box||!w.photographerId)return;saveWizard();const sv=(state.bootstrap?.services||[]).filter(x=>w.services.includes(x.code)),minutes=sv.reduce((n,x)=>n+Number(x.duration_min||0),0)||90,start=`${w.date}T${w.time}:00`,approx=new Date(`${w.date}T${w.time}:00+10:00`),end=new Date(approx.getTime()+minutes*60000).toISOString();box.innerHTML='Checking the selected photographer’s connected calendar…';try{const d=await api('/api/calendar/freebusy',{method:'POST',body:JSON.stringify({photographerId:w.photographerId,start,end})});if(!d.connected)box.innerHTML=`${ico('alert')} <strong>Calendar not connected.</strong> Northlight will still enforce the photographer’s working hours, but external busy events cannot be checked.`;else if(d.busy?.length)box.innerHTML=`${ico('alert')} <strong>Busy in Google Calendar.</strong> Choose another time.`;else box.innerHTML=`${ico('check')} <strong>Available in connected Google Calendar.</strong> Final working-hours and buffer rules are rechecked when you create the task.`}catch(e){box.textContent=e.message}}
async function createTask(){const w=state.wizard;if(w.submitting)return;w.submitting=true;drawWizard();try{const d=await api('/api/tasks',{method:'POST',body:JSON.stringify({property:w.property,address:w.address,suburb:w.suburb,area:w.area,agentId:w.agentId,services:w.services,photographerId:w.photographerId,scheduledStart:`${w.date}T${w.time}`,priority:w.priority,idempotencyKey:w.idempotencyKey})});closeOverlay();await bootstrap();render();const hs=d.handoffs||{},summary=[hs.dropbox==='done'?'Dropbox ✓':'Dropbox pending',hs.calendar==='done'?'Calendar ✓':hs.calendar==='not_connected'?'Calendar not connected':'Calendar attention',hs.email==='done'?'Email ✓':'Email attention'].join(' · ');toast(d.reused?'Existing task resumed':'Task created',`${d.task.task_no} · ${summary}`);openTask(d.task.id)}catch(e){toast('Could not create task',e.message);w.submitting=false;drawWizard()}}
function closeOverlay(){$('#overlay').innerHTML=''}
function nextCopy(s){return({assigned:'Photographer needs to confirm the booking.',confirmed:'The shoot is booked. Northlight is waiting for capture.',shoot_complete:'Source media is expected next.',raw_received:'Editor needs to begin post-production.',editing:'Editing is in progress.',review:'Agent needs to review the edited media.',revision:'Editor is working on requested changes.',delivered:'Final media is delivered.',declined:'Management needs to assign another photographer.',cancelled:'This task is closed.'})[s]||'Keep the task moving to the next stage.'}
function workflowActions(t){const r=state.session.role,out=[];if(t.status==='assigned'&&(r==='photographer'||['admin','owner'].includes(r)))out.push(['confirm','Confirm booking','primary'],['decline','Decline','secondary']);if(['assigned','confirmed','shoot_complete'].includes(t.status)&&(r==='photographer'||['admin','owner'].includes(r)))out.push(['source_ready','Source upload complete','primary']);if(['raw_received','revision'].includes(t.status)&&(r==='editor'||['admin','owner'].includes(r)))out.push(['start_editing',t.status==='revision'?'Start revision':'Start editing','primary']);if(t.status==='editing'&&(r==='editor'||['admin','owner'].includes(r)))out.push(['submit_review','Submit for review','primary']);if(t.status==='review'&&(r==='agent'||['admin','owner'].includes(r)))out.push(['request_revision','Request revision','secondary'],['approve_delivery','Approve final delivery','primary']);return out}
function uploadStages(t){const r=state.session.role;if(['admin','owner'].includes(r))return['01_RAW','02_EDITED','03_FINAL','04_REFERENCE'];if(r==='agent'&&t.agent_user_id===state.session.userId)return['04_REFERENCE'];if(r==='photographer'&&t.photographer_user_id===state.session.userId)return['01_RAW','04_REFERENCE'];if(r==='editor'&&t.editor_user_id===state.session.userId)return['02_EDITED','04_REFERENCE'];return[]}
async function openTask(id){try{const d=await api(`/api/tasks/${id}`),t=d.task;state.selected=t;const agent=person(t.agent_user_id),ph=person(t.photographer_user_id),ed=person(t.editor_user_id),actions=workflowActions(t),stages=uploadStages(t),management=['admin','owner'].includes(state.session.role);$('#overlay').innerHTML=`<div class="drawer-bg" id="drawerBg"><aside class="drawer"><div class="drawer-head"><div><div class="eyebrow">${esc(t.task_no)} · ${esc(t.status)}</div><h2>${esc(t.property_name)}</h2></div><button class="btn secondary icon-btn" id="closeDrawer">×</button></div><div class="drawer-content"><div class="next"><small>What happens next</small><h3>${esc(t.next_action||nextCopy(t.status))}</h3><p>Booking, files, communication and external changes stay attached to this record.</p></div>${actions.length?`<div class="card section-card"><h3>Move the task</h3><div class="toolbar">${actions.map(([a,l,c])=>`<button class="btn ${c}" data-status-action="${a}">${l}</button>`).join('')}</div></div>`:''}<div class="card section-card"><h3>Booking</h3><p>${fmt(t.scheduled_start)}<br>${esc(t.address)} · ${esc(t.suburb)}<br>${(t.service_codes||[]).map(serviceName).join(' · ')}</p><div class="toolbar">${statusBadge(t.status)}<span class="badge">${esc(t.priority)}</span>${!['delivered','cancelled'].includes(t.status)&&['admin','owner','agent','photographer'].includes(state.session.role)?'<button class="btn secondary" id="rescheduleTask">Reschedule</button>':''}${!['delivered','cancelled'].includes(t.status)&&['admin','owner','agent'].includes(state.session.role)?'<button class="btn danger" id="cancelTask">Cancel</button>':''}</div></div><div class="card section-card"><h3>People</h3><p><strong>Agent</strong> · ${esc(agent?.name||'—')}<br><strong>Photographer</strong> · ${esc(ph?.name||'—')}<br><strong>Editor</strong> · ${esc(ed?.name||'Not assigned')}</p></div><div class="card section-card"><div class="card-head compact"><h3>${ico('files')}Secure media</h3>${management?'<button class="btn secondary" id="refreshDropbox">Refresh from Dropbox</button>':''}</div>${management?`<p class="detail-copy">${t.dropbox_path?`Workspace: ${esc(t.dropbox_path)}`:'Dropbox workspace is not yet confirmed for this task.'}</p>`:''}<div id="secureFiles"><div class="empty">Loading allowed media…</div></div>${stages.length?uploadPanel(t,stages):''}</div><div class="card section-card"><h3>Conversation</h3><div id="comments"><div class="empty">Loading conversation…</div></div><div class="field"><textarea id="commentText" rows="3" placeholder="Add a task comment…"></textarea></div><button class="btn primary" id="sendComment">${ico('message')}Add comment + email</button></div><div class="card section-card"><h3>Raise an issue</h3><p class="detail-copy">Track a problem against the property instead of losing it in email or chat.</p><button class="btn secondary" id="raiseIssue">${ico('alert')}Raise issue</button></div><div class="card section-card"><h3>Activity</h3><div class="timeline">${(d.events||[]).map(e=>`<div class="event"><i></i><div><strong>${esc(e.detail?.message||String(e.type).replaceAll('_',' '))}</strong><small>${fmt(e.created_at)}</small></div></div>`).join('')||'<div class="empty">No activity yet.</div>'}</div></div></div></aside></div>`;$('#closeDrawer').onclick=closeOverlay;$('#drawerBg').onclick=e=>{if(e.target.id==='drawerBg')closeOverlay()};$$('[data-status-action]').forEach(b=>b.onclick=()=>taskAction(t.id,b.dataset.statusAction));if($('#rescheduleTask'))$('#rescheduleTask').onclick=()=>openReschedule(t);if($('#cancelTask'))$('#cancelTask').onclick=()=>cancelTask(t);if($('#refreshDropbox'))$('#refreshDropbox').onclick=()=>refreshDropbox(t.id);$('#sendComment').onclick=()=>sendComment(t.id);$('#raiseIssue').onclick=()=>raiseIssue(t.id);if($('#uploadBtn'))$('#uploadBtn').onclick=()=>uploadSelected(t);await Promise.all([loadComments(t.id),loadFiles(t.id)])}catch(e){toast('Could not open task',e.message)}}
function uploadPanel(t,stages){return`<div class="upload-panel"><div class="eyebrow">Upload to Dropbox</div><div class="upload-grid"><select id="uploadStage">${stages.map(x=>`<option value="${x}">${x.replace(/^\d+_/,'').replaceAll('_',' ')}</option>`).join('')}</select><select id="uploadService">${(t.service_codes||[]).map(x=>`<option value="${x}">${esc(serviceName(x))}</option>`).join('')}<option value="">Reference / no service</option></select><input id="uploadFile" type="file"><button class="btn secondary" id="uploadBtn">Upload</button></div><small>Files upload directly to the connected Dropbox workspace; Northlight does not store the media payload.</small></div>`}
async function loadFiles(id){const box=$('#secureFiles');if(!box)return;try{const d=await api(`/api/tasks/${id}/files`),groups={};for(const f of d.files||[]){(groups[f.stage]??=[]).push(f)}box.innerHTML=(d.stages||[]).map(stage=>`<div class="file-stage"><div class="file-stage-head"><strong>${stage.replace(/^\d+_/,'').replaceAll('_',' ')}</strong><span class="badge">${groups[stage]?.length||0}</span></div>${(groups[stage]||[]).map(f=>`<button class="file-row" data-file="${f.id}"><span>${ico(f.file_type==='file'?'files':'folder')}</span><div><strong>${esc(f.name)}</strong><small>${esc(serviceName(f.service_code)||f.service_code||'')}</small></div><span>${f.size_bytes?formatBytes(f.size_bytes):''}</span></button>`).join('')||'<div class="file-empty">No files yet.</div>'}</div>`).join('')||'<div class="empty">No media stages are available for this role.</div>';$$('[data-file]').forEach(b=>b.onclick=()=>openSecureFile(b.dataset.file))}catch(e){box.innerHTML=`<div class="hint">${esc(e.message)}</div>`}}
function formatBytes(n){n=Number(n||0);if(n<1024)return`${n} B`;if(n<1048576)return`${(n/1024).toFixed(1)} KB`;if(n<1073741824)return`${(n/1048576).toFixed(1)} MB`;return`${(n/1073741824).toFixed(1)} GB`}
async function openSecureFile(fileId){try{const d=await api('/api/files/link',{method:'POST',body:JSON.stringify({fileId})});window.open(d.url,'_blank','noopener,noreferrer')}catch(e){toast('Could not open file',e.message)}}
async function uploadSelected(t){const input=$('#uploadFile'),file=input?.files?.[0];if(!file)return toast('Choose a file first');const stage=$('#uploadStage').value,service=stage==='04_REFERENCE'?'':$('#uploadService').value;if(stage!=='04_REFERENCE'&&!service)return toast('Choose the media service');const btn=$('#uploadBtn');btn.disabled=true;btn.textContent='Uploading…';try{const prep=await api('/api/files/upload-link',{method:'POST',body:JSON.stringify({taskId:t.id,stage,serviceCode:service,filename:file.name})}),r=await fetch(prep.url,{method:'POST',headers:{'content-type':'application/octet-stream'},body:file});if(!r.ok)throw new Error(`Dropbox upload failed (${r.status})`);await api('/api/files/complete-upload',{method:'POST',body:JSON.stringify({taskId:t.id,path:prep.path})});toast('Uploaded to Dropbox',file.name);input.value='';await loadFiles(t.id)}catch(e){toast('Upload needs attention',e.message)}finally{btn.disabled=false;btn.textContent='Upload'}}
async function refreshDropbox(taskId){try{const d=await api('/api/dropbox/sync',{method:'POST'});toast('Dropbox refreshed',`${d.changed||0} new or changed file(s).`);await loadFiles(taskId)}catch(e){toast('Dropbox needs attention',e.message)}}
async function taskAction(id,action){try{const d=await api(`/api/tasks/${id}/status`,{method:'POST',body:JSON.stringify({action})});toast('Task updated',d.message||'Workflow moved forward.');await bootstrap();await openTask(id)}catch(e){toast('Could not update task',e.message)}}
function openReschedule(t){const local=new Intl.DateTimeFormat('en-CA',{year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',hourCycle:'h23',timeZone:'Australia/Melbourne'}).formatToParts(new Date(t.scheduled_start)),p=Object.fromEntries(local.filter(x=>x.type!=='literal').map(x=>[x.type,x.value])),date=`${p.year}-${p.month}-${p.day}`,time=`${p.hour}:${p.minute}`;$('#overlay').insertAdjacentHTML('beforeend',`<div class="modal-bg top-modal" id="rescheduleModal"><div class="modal small-modal"><div class="modal-head"><div><div class="eyebrow">${esc(t.task_no)}</div><h2>Reschedule</h2></div><button class="btn secondary icon-btn" id="closeReschedule">×</button></div><div class="modal-body"><div class="field"><label>Date</label><input type="date" id="rDate" value="${date}"></div><div class="field"><label>Time</label><input type="time" id="rTime" value="${time}"></div><button class="btn primary block" id="saveReschedule">Update booking</button></div></div></div>`);$('#closeReschedule').onclick=()=>$('#rescheduleModal').remove();$('#saveReschedule').onclick=async()=>{const start=new Date(`${$('#rDate').value}T${$('#rTime').value}:00+10:00`),duration=Math.max(30,(new Date(t.scheduled_end)-new Date(t.scheduled_start))/60000||90),end=new Date(start.getTime()+duration*60000);try{await api(`/api/tasks/${t.id}/schedule`,{method:'PATCH',body:JSON.stringify({start:start.toISOString(),end:end.toISOString()})});$('#rescheduleModal').remove();toast('Booking updated','Google Calendar was updated when connected.');await bootstrap();openTask(t.id)}catch(e){toast('Could not reschedule',e.message)}}}
async function cancelTask(t){if(!confirm(`Cancel ${t.task_no} · ${t.property_name}?`))return;try{await api(`/api/tasks/${t.id}/schedule`,{method:'DELETE'});toast('Task cancelled');closeOverlay();await bootstrap();render()}catch(e){toast('Could not cancel task',e.message)}}
async function loadComments(id){try{const d=await api(`/api/comments?taskId=${encodeURIComponent(id)}`),box=$('#comments');if(box)box.innerHTML=(d.comments||[]).map(c=>`<div class="comment"><strong>${esc(c.author_name||'Northlight user')}</strong><p>${esc(c.body)}</p><small>${fmt(c.created_at)}</small></div>`).join('')||'<div class="empty">No comments yet.</div>'}catch(e){const box=$('#comments');if(box)box.innerHTML=`<div class="hint">${esc(e.message)}</div>`}}
async function sendComment(id){const text=$('#commentText').value.trim();if(!text)return;try{await api('/api/comments',{method:'POST',body:JSON.stringify({taskId:id,body:text})});$('#commentText').value='';await loadComments(id);toast('Comment added','Email notification attempted.') }catch(e){toast('Comment failed',e.message)}}
async function raiseIssue(id){const subject=prompt('Issue title');if(!subject)return;const description=prompt('Describe what needs attention')||'';try{await api('/api/issues',{method:'POST',body:JSON.stringify({taskId:id,subject,description,priority:'normal'})});toast('Issue raised','Visible to the property team and Owner/Admin.');await bootstrap();openTask(id)}catch(e){toast('Issue failed',e.message)}}
async function openPerson(id){if(!['admin','owner'].includes(state.session.role))return;try{const d=await api(`/api/team/${id}`),u=d.user,p=d.provider,e=d.editor,c=d.calendar;$('#overlay').innerHTML=`<div class="drawer-bg" id="drawerBg"><aside class="drawer person-drawer"><div class="drawer-head"><div><div class="eyebrow">${esc(u.role_code)} · team member</div><h2>${esc(u.name)}</h2></div><button class="btn secondary icon-btn" id="closeDrawer">×</button></div><div class="drawer-content"><div class="person-hero"><div class="avatar large">${initials(u.name)}</div><div><h3>${esc(u.name)}</h3><p>${esc(u.email)}${u.phone?` · ${esc(u.phone)}`:''}</p><span class="badge">${d.activeCount} active</span></div></div>${p?`<div class="card section-card"><h3>Photographer coverage</h3><p class="detail-copy"><strong>Areas</strong><br>${esc((p.areas||[]).join(' · ')||'Not configured')}<br><br><strong>Services</strong><br>${(p.service_codes||[]).map(serviceName).join(' · ')||'Not configured'}<br><br><strong>Base</strong> · ${esc(p.home_base||'Melbourne')} · ${p.service_radius_km||25} km<br><strong>Travel buffer</strong> · ${p.travel_buffer_min||15} min</p></div>`:''}${e?`<div class="card section-card"><h3>Editor profile</h3><p class="detail-copy"><strong>Skills</strong><br>${(e.skills||[]).map(serviceName).join(' · ')||'Not configured'}<br><br><strong>Current load</strong> · ${e.current_load||0}</p></div>`:''}${['photographer','agent'].includes(u.role_code)?`<div class="card section-card"><h3>${ico('calendar')}Google Calendar</h3><p class="detail-copy">${c?.status==='connected'?`Connected · ${esc(c.account_label||'Google')}`:'Not connected'}</p></div>`:''}<div class="card"><div class="card-head"><h3>Recent assignments</h3></div><div class="list">${(d.tasks||[]).map(taskRow).join('')||'<div class="empty">No assignments yet.</div>'}</div></div></div></aside></div>`;$('#closeDrawer').onclick=closeOverlay;$('#drawerBg').onclick=e=>{if(e.target.id==='drawerBg')closeOverlay()};$$('[data-task]').forEach(b=>b.onclick=()=>openTask(b.dataset.task))}catch(e){toast('Could not load team member',e.message)}}
function openAddTeam(){$('#overlay').innerHTML=`<div class="modal-bg"><div class="modal small-modal"><div class="modal-head"><div><div class="eyebrow">Admin · team provisioning</div><h2>Add team member</h2></div><button class="btn secondary icon-btn" id="closeModal">×</button></div><form class="modal-body" id="teamForm"><div class="field"><label>Name</label><input id="tmName" required></div><div class="field"><label>Email</label><input id="tmEmail" type="email" required></div><div class="field"><label>Phone</label><input id="tmPhone"></div><div class="field"><label>Role</label><select id="tmRole"><option value="agent">Agent</option><option value="photographer">Photographer</option><option value="editor">Editor</option><option value="owner">Owner</option></select></div><div class="field"><label>Temporary password</label><input id="tmPassword" type="password" minlength="10" required></div><button class="btn primary block">Create credentials</button></form></div></div>`;$('#closeModal').onclick=closeOverlay;$('#teamForm').onsubmit=async e=>{e.preventDefault();const btn=$('#teamForm button');btn.disabled=true;try{await api('/api/admin/users',{method:'POST',body:JSON.stringify({name:$('#tmName').value,email:$('#tmEmail').value,phone:$('#tmPhone').value,role:$('#tmRole').value,password:$('#tmPassword').value})});toast('Team member created','Credentials are ready. Configure coverage/skills next.');closeOverlay();await bootstrap();render()}catch(err){toast('Could not create team member',err.message);btn.disabled=false}}}
function connect(p){location.href=`/api/oauth/${p}/start`}
async function testIntegration(p){try{const d=await api(`/api/integrations/${p}/test`,{method:'POST'});toast(`${p[0].toUpperCase()+p.slice(1)} ready`,d.message||'Connection verified.');await bootstrap();renderPage()}catch(e){toast(`${p} needs attention`,e.message)}}
(async function init(){try{const d=await api('/api/auth/session');state.session=d.session;if(state.session)await bootstrap()}catch{}render()})();
